# 当前交接

最后更新：2026-09-16。
当前重点：M0-06（文档、合同与源码差异校正）以及 M0-04 的全站状态核对。
目标分支：`main`；所有工作继续通过独立分支、CI 和 Pull Request 合并。

## 用户当前目标

持续推进 OrionAdmin 开发路线。先完成 P0 基线、导航、文档和测试，再推进正式发布能力、管理组件维护、完整更新回滚，以及炮塔、舰船、活动和星区建设。只有遇到真实服务器操作、破坏性数据变更、权限或产品取舍时再请求用户配合。

## 已完成基线

- PR #1～#7：仓库接续、CI、前端去重复、样式分区和后端职责拆分。
- 提交 `5e3f9df`：M0-11 测试 fixture/test-double 与主验证流程分离。
- 提交 `2825be4`：移除 Server Update 兼容转发层和重复错误 helper。
- PR #11：配置预检失败和请求错误靠近操作区显示；RCON 密码状态准确；移除冗余“文件与配置”一级入口。CI 已通过并合并为 `49dadb5`。

## 当前基线结论

服务器安装、初始化、控制、性能、内存与星区、自动任务、备份恢复、日志、诊断、更新检查/校验/安全点，以及玩家、联盟、奖励和受限 Inventory 已有真实代码与自动验证。完整服务器更新执行、从更新安全点恢复、管理组件安装维护、正式 Windows 发布流程仍未开放。

当前源码仍有四个没有真实路由的一级导航入口（总览、舰船管理、MOD 管理、设置）；在对应最小功能完成前应隐藏。管理组件 README 版本和部分接续信息需要与源码同步，架构检查需要覆盖后来新增的 API/Lua 模块。

## 下一步顺序

1. 完成导航真实性、管理组件版本、接续文档和架构边界同步，并通过现有 CI。
2. 对齐 OpenAPI 与实际端点，加入自动合同漂移检查。
3. 让 Playwright/视觉 QA 脱离本机硬编码路径，并把关键预检、错误态和导航冒烟测试接入 CI。
4. 建立隔离 Galaxy 的当前版本验收基线；这一步需要用户提供或确认可测试的服务器实例时再进行。
5. 完成 P0 后进入 REL-02 发布服务化、REL-01 管理组件安装维护和完整更新/回滚闭环。

历史测试通过不能替代当前提交的 CI；源码存在也不能替代真实游戏验收。
