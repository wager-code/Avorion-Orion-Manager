# 当前交接

最后更新：2026-09-16。
当前重点：M0-02～M0-05（更新流程、共享 Inventory、全站状态和隔离 Galaxy 验收）。
目标分支：`main`；所有工作继续通过独立分支、CI 和 Pull Request 合并。

## 用户当前目标

持续推进 OrionAdmin 开发路线。先完成 P0 基线、导航、文档和测试，再推进可重复 Windows 发布/服务化、管理组件安装维护、完整更新与安全点恢复，之后进入普通炮塔、舰船、活动和星区建设。只有遇到真实服务器操作、破坏性数据变更、权限或产品取舍时再请求用户配合。

用户已明确授权：本任务后续 CI 通过、无冲突且范围核对无误的 Pull Request 可以合并；不得强推或绕过失败检查。

## 已完成基线

- PR #1～#7：仓库接续、CI、前端去重复、样式分区和后端职责拆分。
- 提交 `5e3f9df`：测试 fixture/test-double 与主验证流程分离。
- PR #11：配置预检错误反馈、RCON 密码状态与“文件与配置”导航修复。
- PR #12：隐藏没有真实路由的一级入口，同步架构边界、管理组件版本和开发文档。
- PR #13：OpenAPI 补齐真实健康、Operation、管理桥、Inventory 目录/图标和更新安全点接口；加入合同漂移验证。
- PR #14：29 个浏览器 QA/截图脚本移除个人 Playwright、浏览器和仓库路径；统一运行时发现，并在 CI 中检查语法与路径可移植性。

以上 PR 均由 Windows CI 验证架构边界、.NET Release 构建、后端验证、前端类型检查、生产构建和 Sites 测试后合并。

## 当前基线结论

服务器安装、初始化、控制、性能、内存与星区、自动任务、备份恢复、日志、诊断、更新检查/校验/安全点，以及玩家、联盟、奖励和受限 Inventory 已有真实代码与自动验证。一级导航只保留有真实页面的服务器、玩家、联盟和游戏管理。

完整服务器更新执行、从安全点恢复、OrionAdminBridge 安装维护和正式 Windows 发布流程仍未开放。全站真实空态/降级态、共享 Inventory 两档视口，以及当前代码对隔离 Galaxy 的端到端验收仍需运行环境证据，不能用历史截图或 Mock 替代。

## 下一步顺序

1. 在可运行环境执行 M0-02～M0-04 浏览器 QA，核对更新流程、玩家/联盟共享 Inventory、空态与降级态。
2. 用户确认隔离服务器实例、SteamCMD、AvorionServer、Galaxy 路径和允许的启停/写入范围后，完成 M0-05 当前版本真实验收。
3. 完成 P0 后依次推进 REL-02 可重复发布与服务化、REL-01 管理组件安装维护、REL-04 完整更新/安全点恢复闭环。
4. 正式运行基础完成后进入 M1 普通炮塔预览与单件发放，再推进舰船、活动和星区建设。

历史测试通过不能替代当前提交的 CI；源码存在也不能替代真实游戏验收。
