# 关键决策记录

- 2026-09-07 用户明确改为专用服务端管理 MOD 版本，并授权 M1 基础验证。旧版“禁止自定义管理 MOD”决策由此针对 OrionAdminBridge 解除；第三方 MOD 写操作范围不扩大。先在隔离 E2E 服务端验证握手、在线玩家只读查询与异常恢复，再逐页确认 UI。详见 `17_MANAGEMENT_MOD_STAGE_M1.md`。

- 产品从 Windows WinForms 启动器升级为完整 Avorion Web 管理中心。
- 不再以传统桌面客户端为主。
- 管理核心运行在游戏服务器本机：Server Agent + Web + 专用 OrionAdminBridge；MOD 能力逐项验证开放。
- 浏览器远程访问，目标服务器可部署在香港等非大陆节点。
- 产品要支持“新服务器全自动部署”和“导入已有服务器”。
- 用户最看重 UI 颜值与一致性。
- 用户选择 A：浅色 Fluent 企业后台方向。
- 视觉定位：70% Web 管理后台 + 30% 桌面客户端质感。
- 后续 UI 一页一页确认。
- 没有确认的 Avorion API 功能不做。
- 星区重置不做。
- 现有舰船直接转让不做。
- 内存管理做官方可支持的安全星区卸载与监控，不做虚假 RAM 清理。
- 游戏活动中心作为差异化核心功能保留。
- 服务器管理八个子页面 UI 已于 2026-08-30 逐页确认。
- 后续不等待全套模块 UI 全部完成；采用“一个模块 UI 确认 → API Contract → 真实数据闭环 → 下一个模块”的纵向开发方式。
- 服务器管理正式开发先做 R0 基础设施和 R1 只读真实数据；高风险写操作在命令验证、测试环境和回滚方案准备完成后再实现。
- 真实数据不可取得时必须显示未知或不可用，禁止沿用 Mock 数值或默认正常状态。
- 服务器管理 R0/R1 后端已于 2026-08-31 完成：真实只读采集、SQLite 历史、诊断队列和禁止假数据回退均已验证；前端须在真实服务器配置和联调后再移除 Mock。
- 未完成认证、命令验证、测试环境和失败回滚前，所有服务器控制、更新、恢复等高风险写操作继续返回 `COMMAND_NOT_VERIFIED`，不得从 UI 解锁。
- SteamCMD 安装、Avorion 服务端安装、已有路径导入、自动检测和路径管理统一归属“服务器管理 → 更新”，不增加永久侧边栏入口。
- `/server/update` 必须按真实环境状态切换“未配置、部分配置、检测确认、验证、正常更新、配置向导、执行、成功、失败恢复”等内容；路径已完整且验证通过时直接显示正常更新页。
- 安装/导入后的服务器管理配置使用三步向导：Galaxy 与基础信息、网络与 RCON、检查与启动。
- 服务器安装与更新流程共 10 个界面状态已于 2026-08-31 逐页确认并归档；本轮只固化 UI 与流程，未实现浏览器代码或解锁写操作后端。
- 已创建 Figma 确认稿索引 `Avorion 服务器安装与更新流程（已确认）`，用于集中评审 10 个状态，不将其误记为正式组件库或已实现页面。
- 2026-08-31 用户决定保留当前已确认的 A 风格，不采用后续生成的 A Pro 重设计方案；下一阶段优先补齐现有页面、路由与交互闭环。
- 现有按钮必须满足三选一：真实前端交互/页面跳转、明确禁用并说明原因、或移除；不得保留点击无反应或用假成功掩盖未接入后端的按钮。
- “全新安装”和“选择已有路径”已实现为可运行前端流程：路径填写、格式校验、执行/验证前确认、URL 恢复和会话草稿恢复均已接通；真实路径验证已经解锁，真实安装按钮继续等待 Server Agent 写操作安全能力。
- 2026-08-31 更新环境的只读真实闭环已接通：浏览服务器目录、常用路径/Steam 库有限扫描、SteamCMD 与 AvorionServer.exe 存在性和读取权限验证、版本线索与磁盘可用空间读取，以及验证通过后的本机 SQLite 路径保存。检测不到或验证失败时必须停留在未配置/待修正状态，不允许显示假成功。
- 全盘递归搜索、目录创建、SteamCMD 下载、服务端安装和更新执行仍未开放；下一写操作阶段必须先补齐认证/CSRF、命令白名单、幂等 Operation、进度恢复和失败清理方案。
- 2026-08-31 R2 写操作安全底座已完成：只允许 Agent 本机建立短期 HttpOnly/SameSite 管理员会话；全部非 GET API 强制校验会话、CSRF 与本机 Origin；Operation 支持幂等键、请求指纹、进度持久化以及 Agent 重启后把未完成任务标记失败。
- 服务器命令执行采用固定清单和默认拒绝策略。当前清单内所有启动、保存、停服、重启、强停、SteamCMD 安装、服务端安装/更新和备份恢复命令均保持 `verified=false`；完成单项命令调查、测试目录演练和清理/回滚验证前不得解锁。
- 本机管理员会话不是公网账号系统；未完成 TLS、持久账号认证、设备/角色授权和登录保护前，Server Agent 继续只监听回环地址，不允许直接公网暴露。
- 2026-09-01 首个真实写命令 `steamcmd.install` 已单独解锁：目标必须是父目录已存在的全新 Windows 本地目录，不允许覆盖、磁盘根目录、UNC/设备路径、符号链接或目录联接；固定从 Valve Steam CDN 下载，限制归档体积与解压范围，记录 SHA-256，必须通过 Valve Authenticode 签名，随后使用同父目录暂存并原子移动。
- SteamCMD 安装完成只代表 `steamcmd.exe` 引导程序安全落盘；本阶段明确不运行它的首次自更新，不安装 Avorion 服务端，也不解锁 `avorion.install`。下载、校验或签名失败时删除暂存文件并保持目标目录不存在。
- 2026-09-01 `avorion.install` 已完成单独验证并解锁：仅允许已通过 `Valve` 或 `Valve Corp.` Authenticode 的本地 SteamCMD；固定匿名登录、App `565060`、显式 `public` 分支、`validate` 和退出参数，且参数使用 `ProcessStartInfo.ArgumentList`，不经过 Shell。
- Avorion 服务端安装必须使用与 SteamCMD 互不包含的全新本地目录，在同父目录暂存；只有 SteamCMD 退出码为 0、返回 App 565060 精确成功标记，并且 `bin/AvorionServer.exe` 与 `bin/ServerRunner.exe` 同时存在后才原子落盘。失败时删除暂存目录，不覆盖目标，也不启动服务端。
- 真实端到端演练首先暴露了 `Missing configuration`，据 Avorion 开发者当前建议加入固定 `-beta public` 后成功；同时验证到 SteamCMD 自更新版签名主体为 `Valve Corp.`，因此白名单只允许旧主体 `Valve` 与新主体 `Valve Corp.`。
- 2026-09-01 服务器配置向导已接入本机 SQLite 非敏感草稿和真实执行前预检：检查已保存更新环境、Galaxy 路径、字段约束与本机 TCP/UDP 端口占用；RCON 密码仅存在于单次预检请求内存，不入库、不记录、不回传。
- 2026-09-01 `server.configure` 已按安全边界解锁：每次应用前重新预检并确认精确服务端程序已停止；始终原子写入不含秘密的受控启动档案；只有已有 Galaxy 且 `server.ini` 已存在、目标节与键无重复歧义时，才原子更新已验证字段并校验结果，失败则回滚。
- 新 Galaxy 在首次启动前没有 `server.ini`，因此配置应用只保存可验证的启动参数，不伪造该文件，也不宣称 RCON 已配置。游戏监听地址、独立 Query 端口、防火墙与启动服务端仍分别保持锁定并显示为后续处理。
- 2026-09-01 `server.initialize` 已按“新 Galaxy 专用”的窄范围解锁：从已验证启动档案启动精确服务端程序，等待其自行生成 `server.ini`，通过控制台依次发送 `/save` 与 `/stop`，确认安全退出后才原子写入 RCON，再重启并要求真实 RCON 认证成功。
- 首次初始化的 RCON 密码必须由用户再次输入；前一步配置 Operation 完成时浏览器已清除密码，后端也不持久化。密码只进入本次工作内存及 Avorion 必需的 `server.ini`，请求证据、Operation 结果、日志和前端 URL 均不得包含秘密。
- `server.initialize` 的进程控制、标准输入顺序、INI 写入与 Source RCON 数据包认证已通过独立 `AvorionServer` 集成测试程序完成端到端验证；正式 Avorion 2.5.13 二进制仍需在用户完成真实安装后做现场兼容性复核。普通控制页启动/保存/停服/重启并未因此自动解锁。
- 2026-09-05 用户明确决定不采用任何自定义 Avorion 管理 MOD：系统不得植入、安装、上传、修补或启用自定义 Lua MOD。服务器配置向导从四步收敛为三步（Galaxy 与基本信息、网络与 RCON、检查与启动）；兼容字段 `installManagementMod` 永远为 `false`。
- 2026-09-06 更新页只先解锁两个窄范围真实命令：`avorion.update.check` 在 Valve 签名验证后以匿名固定参数查询 App 565060 public Build ID；`avorion.files.verify` 只读校验 `AvorionServer.exe` 与 `ServerRunner.exe` 的 PE 文件头、大小和 SHA-256。两者均持久化 Operation 并支持切页恢复；不得调用 `app_update validate`。完整更新仍需备份、停服、回滚与健康检查链验证，继续锁定。
- `MOD 管理` 只读取用户服务器已经存在或已经配置的 MOD/Workshop ID，并仅展示能从真实文件、配置、Workshop 元数据或日志验证的状态；安装、启停、更新、删除等写操作不在当前范围，依赖自定义 MOD 才能实现的能力必须显示不可用而不是伪造数据。
- 2026-09-06 `avorion.update.rollback-point.create` 已按独立安全边界解锁：服务端必须已停止，已保存更新环境必须与受管启动档案一致；安全点同时包含完整服务端程序目录与当前 Galaxy，拒绝任何符号链接/目录联接，先复制到同盘暂存目录并记录每个文件的 SHA-256，全量重读复核与清单校验通过后才原子发布。该安全点不是 Avorion 自动 `.bak`，不得混入“服务器管理 → 备份”；恢复执行和完整 `avorion.update` 仍保持锁定。
