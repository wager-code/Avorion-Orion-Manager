# 用户已确认的要求（新助手必须继承）

## 产品与开发方式

- 软件是 Avorion 专用中文浏览器管理后台；运行核心位于游戏服务器所在电脑：Web + .NET Server Agent + 专用管理 MOD。
- 不做传统展示网页，不改回 WinForms/Windows 客户端；同一电脑、局域网测试不要求公网 IP。公网远程访问的 HTTPS、账户和安全部署仍需正式完成。
- 用户曾明确放弃 UI 大改，要求沿现有 A 风格继续完善页面与功能。
- 页面逐个规划，先给高保真 UI 图片确认，再写该页浏览器代码；一次只展示一个子页面。不要一口气开发四大模块或整套软件。
- 实现后必须实际运行和截图，检查 1920×1080、1440×900 的配色、溢出、间距、对齐、滚动与响应式；构建成功不等于 UI 完成。
- 正文工作区可以纵向滚动；不要为挤进一屏删除已确认内容。
- 新页面优先正确确定数据来源，再设计可实现的界面；不编造游戏字段，不使用假数据冒充成功。旧原型中的 Mock 许可已被后续真实接入要求取代。
- 采用成熟 UI 组件和统一 Design System，复用现有导航、顶栏、Tabs、卡片、字体、间距、按钮。
- 页面职责必须清晰，避免跨页面重复完整呈现同一信息。玩家在线状态等玩家域信息以「玩家管理」为归属；其他页面只有在完成本页核心任务确实依赖时，才能展示最小必要的关联上下文，并明确它服务于哪个操作或判断。不得为了填充页面或制造“丰富感”重复堆叠已有摘要、列表或状态。

## 设计依据及插件

- `references/A_总览母版.png` 为 A 风格母版，`references/服务器_控制_参考.png` 为控制参考；现有 `frontend-prototype/src` 是已实施界面依据。
- 深蓝左侧导航、浅灰底、白卡片、蓝色主按钮，绿/橙/红代表正常/警告/危险。
- 左导航准确名称：总览、服务器管理、玩家管理、联盟管理、游戏管理、舰船管理、MOD 管理、文件与配置、设置。
- 服务器二级页：控制、性能、内存与星区、更新、自动任务、备份、日志、诊断。
- 用户允许并要求合适时主动使用插件：Product Design 用于规划审核/设计图片；Build Web Data Visualization 用于性能等图表；Figma 在需要正式设计稿和组件体系时使用。并非每次需要用户重新 @，也不要为了用而用。
- 新电脑若未安装这些能力，说明可用性；不要假称调用插件或已经完成截图。

## 服务器模块关键业务要求

- 新安装、已有目录检测/复用、选择目录、SteamCMD 路径、服务端路径及后续修改，放在「服务器管理 → 更新」中；不新增永久安装导航。
- 用户可浏览 Agent 所在电脑目录，选父目录并自定义子目录名，无需先手建文件夹或复制路径。
- 目录已存在时，应识别是否为已安装 SteamCMD/服务端并按实际状态处理；无关非空目录不能直接覆盖。
- 路径确认不代表已经更新、移动文件或安装完成；状态只能依真实结果显示。
- 新 Galaxy 配置流程是三步：Galaxy 与基本信息 → 网络与 RCON → 检查与启动。旧四步方案不再适用。
- 安装与长操作归 Agent 执行并持久化 Operation；浏览器刷新、切页、断线后要恢复进度。Agent 真正退出是另一回事，当前未完成任务会标记失败，不能宣称自动续传。
- 失败必须有明确原因和可读日志，不能只有没反应的按钮。
- 备份页读取 Avorion 自动生成的 .bak 文件；从文件获取时间、大小、可用性。不要捏造备份类型/来源分类。
- 更新前安全点与 Avorion .bak 是两回事，不能混入备份记录。
- 真实日志采用白色工作台内深墨蓝阅读区，保留搜索、分类、自动滚动、复制。未经新确认，不增加清空/导出等功能。
- RCON 密码不进入 URL、草稿数据库、日志、Operation 响应；实际游戏要求的 server.ini 中可以存在。
- 暂未验证的危险操作必须保持锁定，不能用弹提示冒充真实操作。

## 最新 MOD 决策（2026-09-07）

- 用户先前反对任何植入，后来明确改为「做 MOD 版本」，并同意专用服务端管理 MOD 的分阶段方案及 M1 基础验证。这是当前有效决策。
- 授权只针对 OrionAdminBridge，不代表可自动安装其他第三方 MOD。
- 专用组件拟在「服务器管理 → 更新」增加安装/版本/连接区；该 UI 尚未出图确认，不应直接加整页。
- 普通「MOD 管理」仍以读取既有 Workshop ID、文件、配置与错误日志为主，第三方写操作未开放。
- 目标是仅服务器安装，玩家不用安装；尚需真实客户端接入验证，不能仅凭 serverSideOnly=true 宣称通过。
- 专用组件必须用官方 Lua API，固定命令白名单、权限检查和可追溯结果；不开放浏览器任意 Lua/RCON 终端。

## 接下来的四大模块

- 玩家：列表/详情、在线状态、联盟、位置，资源/库存/舰船逐项验证；之后消息、踢出/封禁及受支持资源/物品操作。
- 联盟：列表、领袖、成员、等级/权限、资源、库存、舰船，先真实查询再管理操作。
- 舰船：玩家/联盟舰船、位置和状态、货舱/船员/系统/蓝图按需查询；之后从蓝图创建赠送，先单艘后批量。
- 游戏：聊天/事件先行，之后公告、奖励，最后活动中心（海盗袭击、波次防御等）和创建空间站。
- 以上为目标，并非当前已完成能力。不得照早期详细清单一次全开。
- 不擅自加入击杀/死亡/成就、玩家 IP 核心详情、联盟群/官网、战争/占领统计、随意传送、星区重置、现有舰船转移所有权、复杂舰队 AI、完整空间站产业链管理或“假清内存”。
