# 交接包验证记录

日期：2026-09-07。验证对象是本包的干净 ZIP 解压副本，不是原目录里的既有编译产物。
验证在旧电脑的独立目录进行，不等于已经在用户的新电脑测试。

## 已通过

- 比对前端、后端、API 合同和 MOD 的 87 个源码/配置文件 SHA-256：与 D:\管理系统开发 对应文件完全一致。
- 执行交接包 tools/prepare.ps1：后端 Release 构建成功，0 警告、0 错误；pnpm 11.19.0 使用冻结锁文件安装依赖成功；TypeScript 检查和 Vite 生产构建成功。
- .NET 隔离自动测试：PASS，包含真实数据边界、写入安全、生命周期、初始化、配置、安装器、无假数据回退及管理桥接协议断言。
- 前端 worker/构建测试：4 项通过，0 失败。
- Windows PowerShell 5.1 调用 tools/run-local.ps1 -ApiPort 5188 -WebPort 4273 -SmokeTest：SMOKE_PASS。API 健康响应、Vite 控制页 HTTP 200、前端 /api 代理均通过；脚本随后关闭自己启动的 API/Vite，两个隔离端口无残留监听。
- 配置/文档凭据模式检查未发现实际密码或私钥；8 张代表性实现截图人工查看。默认 appsettings.json 不含实际服务端路径和密码。
- 包内不含 node_modules、bin/obj/dist、管理数据库、实际 server.ini、游戏程序、Galaxy、备份、实际日志文件或本机浏览器会话。

第一次联网构建被受限执行环境阻止访问 NuGet；放行网络后完整通过。测试第一次被限制读取现有进程归属，Vite 第一次被限制读取父目录；在正常进程权限下重跑通过。未关闭安全检查、未禁用依赖审核，也没有停止原有真实游戏服来规避测试。

## 本次迁移做了什么

保留当前业务源码；另外编写最新用户需求、进度、环境说明、接续提示词，以及与所在目录相对定位的初始化/启动脚本。没有把原机固定 Node 路径或预编译 exe 当作新电脑前提。
启动脚本仅面向本机开发，默认 localhost；不是 Windows 服务安装器，也不是公网生产部署方案。

## 仍不能据此宣称完成

本次没有重新安装游戏或启动新游戏实例，没有进行新的客户端加入/MOD 非空玩家实测，也没有重新进行全部页面视觉验收。
references/implemented 是旧机历史截图，docs/evidence 和 17_MANAGEMENT_MOD_STAGE_M1.md 是旧机阶段证据；不是新电脑上的实时状态。
已知功能缺口和接续顺序见 02_CURRENT_STATUS.md。新电脑仍需下载开发依赖、重新配置路径并验证自己的游戏环境。

## 完整性

MANIFEST.sha256 列出包内除清单自身之外的所有文件；每行格式为 SHA256、两个空格、相对路径。
交付 ZIP 另附 .sha256 校验文件。最终压缩包逐项读取并核对清单，不包含本次验证时重新生成的依赖与运行数据。
