import { createRequire } from "node:module"; import fs from "node:fs"; import path from "node:path"; import { fileURLToPath } from "node:url"; const require=createRequire(import.meta.url); const { launchQaBrowser } = require("../../tools/qa-browser-runtime.cjs"); const dir=path.resolve(path.dirname(fileURLToPath(import.meta.url)),"../qa/m0a-final"); fs.mkdirSync(dir,{recursive:true}); const browser=await launchQaBrowser({headless:true}); for(const v of [{n:"1440x900",w:1440,h:900},{n:"1672x941",w:1672,h:941},{n:"1920x1080",w:1920,h:1080}]){const c=await browser.newContext({viewport:{width:v.w,height:v.h},locale:"zh-CN"}); const p=await c.newPage(); await p.goto("http://127.0.0.1:4173/server/update",{waitUntil:"domcontentloaded",timeout:10000}); await p.screenshot({path:path.join(dir,`server-update-${v.n}.png`),fullPage:true}); await c.close();} await browser.close(); console.log(dir);