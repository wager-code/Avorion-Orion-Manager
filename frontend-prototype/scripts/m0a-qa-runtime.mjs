import fs from "node:fs"; import path from "node:path"; import { fileURLToPath } from "node:url";
export const runId=process.env.QA_RUN_ID||`run-${Date.now()}`; export const runRoot=process.env.QA_RUN_ROOT||path.resolve(path.dirname(fileURLToPath(import.meta.url)),`../qa/harness/runs/${runId}`); export const reportDir=process.env.QA_REPORT_DIR||path.join(runRoot,"reports"); export const shotDir=process.env.QA_SCREENSHOT_DIR||path.join(runRoot,"screenshots"); export const webBase=process.env.QA_WEB_BASE_URL||"http://127.0.0.1:4173"; export const fixture=process.env.QA_FIXTURE_STATE||"unconfigured"; fs.mkdirSync(reportDir,{recursive:true}); fs.mkdirSync(shotDir,{recursive:true}); export function save(test,report){fs.writeFileSync(path.join(reportDir,`${test}.report.json`),JSON.stringify(report,null,2));}
