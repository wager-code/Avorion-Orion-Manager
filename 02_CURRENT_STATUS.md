# 当前状态与接续点

截至 2026-09-10；这是开发中项目，不是所有模块已完成的软件。
完整的“已支持 / 已确认可开发 / 待验证 / 不支持”分类见 `06_CAPABILITY_MATRIX.md`；后续功能完成后必须同步更新该总账。

## 模块化单体基线

- 2026-09-08 已完成第一轮模块化重构，保持单进程、单部署包和同一 SQLite 数据库，不引入微服务。
- API 入口 `Program.cs` 从 1355 行收敛到 47 行，只负责启动、持久化初始化、中间件和模块注册；当前 HTTP 路由拆为 16 个业务端点模块，库存能力由独立 `InventoryEndpoints` 承担。
- 原 872 行 `ApiSupport.cs` 已移除，拆为 `Security/`、`Capabilities/`、`Operations/`、`Infrastructure/`；后台队列和 Worker 仍在同一进程内运行。
- 管理 MOD 的命令脚本已变成薄路由，协议、玩家、玩家资产、联盟、星区逻辑位于 `data/scripts/lib/orionadmin/`，以后新增游戏能力增加独立模块并在路由显式注册。
- 前端 `App.tsx` 只保留会话级状态和通知，页面路由/懒加载已移动到 `src/app/AppRoutes.tsx`；各业务继续由独立 page/component/lib 文件承载。
- `tools/check-architecture.ps1` 固化边界，`01-prepare.cmd` 会在构建前检查，避免接口和功能逻辑再次堆回入口文件。
- 新功能的标准接入顺序与命名规则见 `07_MODULAR_ARCHITECTURE.md`。

## 已实现并验证

- 服务器管理八个子页面已存在，沿用 A 风格；CPU/内存/主机网络等真实采样、历史、实际日志、备份扫描、有限诊断和自动任务已接入。
- SteamCMD 官方下载/签名检查、App 565060 public 安装、环境路径配置、新 Galaxy 初始化、受管启动/保存/安全关闭/安全重启的核心链路已在旧电脑真实验收。
- Steam 官方 Build 查询、本机关键文件 PE/大小/SHA-256 校验；没有用 app_update validate 冒充只读校验。
- 更新前安全点创建已实现：停服复制安装目录与 Galaxy，逐文件哈希验证。完整更新和恢复执行仍未开放。
- 定时保存/定时安全重启有数据库和 Agent 调度实现。旧机器现场验证过任务创建并停用，未完成长期调度/有人在线的全面验收。
- 近期修复：SteamCMD 首次自更新失败的一次有条件重试、安装目标路径恢复、工作目录、中文 INI 编码、RCON 启动稳定观察、Agent 重连同 PID 注册、活动日志共享读取、内存页真实数据、更新状态与诊断受管路径。

## 管理 MOD（本次最新源码）

- `management-mod/OrionAdminBridge` 0.10.0，协议 1，目标 Avorion 2.5.13；命令入口保持薄路由，协议、玩家、玩家资产、玩家奖励、邮件奖励、联盟、联盟资产、Inventory、星区与动作分派均为独立模块，未发布 Workshop。
- hello、在线玩家分页和已加载星区查询已在正式本机服务端运行。普通 Steam 客户端无需安装此组件即可加入；非空玩家名、所在星区及退出后 total=0 的传播均已实测。
- 已开放单个空闲星区的受保护卸载：仅 RCON/控制台可调用，组件会在调用官方 `Galaxy():tryUnloadSector()` 前再次拒绝有在线玩家或未加载的坐标。操作通过持久化 Operation 执行并复核真实加载列表。
- 2026-09-08 实测：玩家位于 `(-447, 47)` 时请求返回 `SECTOR_HAS_PLAYER` 且星区保持加载；玩家退出后同一星区请求成功，加载星区总数由 3 降为 2，服务端 PID 未变化且 RCON 保持连接。
- 后端路由包含管理组件 hello/players/alliances/alliance、星区查询、玩家/联盟资产读取、玩家/联盟 reward-grants 与 `POST /sectors/unload-attempts`。写操作需要本机管理员会话、CSRF、幂等键和精确确认文本；来源/版本/请求编号/参数边界、响应大小与超时均受限。
- 测试通过：未登录 401、错误分页 400、未知功能 404；停服与停用 MOD 返回 503；重新启用恢复；Agent 重启而游戏 PID 不变时查询恢复。
- 自动测试覆盖协议版本、请求关联、截断/错误响应、空页、中文/转义和 RCON 分包重组。测试夹具不等于真实玩家实测。
- 正式本机 Agent 5088 与 Galaxy 已加载 0.10.0；没有安装该组件的其他服务器仍应返回不可用，不得伪造回退数据。
- `/players` 玩家管理页已按用户确认的方案 3 实现：实时人数、在线玩家、所在星区、玩家索引、刷新、详情弹窗和真实玩家事件均已接入。事件 API 支持服务端 `Player` 分类，并加深玩家日志尾部窗口，避免高频 RCON 轮询挤掉加入/离开记录；界面只呈现可识别的真实事件，不展示 SteamID 等敏感原始行。
- 玩家详情已增加只读资产区：Credits 与铁、钛、纳奥石、崔钢、赛安金属、欧格石、阿沃里昂七种资源。2026-09-08 通过正式 Galaxy 的离线玩家 #1 实测返回 Credits 5000 与七个资源字段；未登录、非法索引、未知玩家分别验证为 401/400/404。
- 玩家详情已增加单人奖励操作：每次选择 Credits 或一种矿物，Credits 单次上限 10 亿、单种矿物单次上限 1 亿；提交前二次确认，后端持久化幂等 Operation，管理组件精确返回并校验变更前后资产，连接结果不确定时不会自动重试。2026-09-08 正式 Galaxy 玩家 #1 实测 Credits 5000→5001、铁 0→1；同一铁矿请求重复提交返回同一 Operation，余额没有重复增加，复用幂等键但改变数量返回 409。
- `/alliances` 联盟管理页已按用户确认的方案 2 实现：从全部玩家的联盟所属关系发现联盟，读取联盟领袖、主星区、成员数、在线成员、舰船、空间站、成员职级/位置/在线状态；刷新、联盟选择和成员详情均使用真实数据。正式 Galaxy 实测发现联盟 `#5` 和 1 名成员。
- 联盟详情已增加独立资产弹窗，不新增重复联盟页面：读取 Credits 与七种矿物，并可每次向该联盟增加 Credits 或一种矿物。2026-09-08 正式 Galaxy 联盟 #5 实测 Credits 0→1、钛 0→1；同一钛矿请求重复提交返回同一 Operation，余额没有重复增加，复用幂等键但改变数量返回 409。
- 联盟页原始方案对照、成员交互及响应式检查继续通过；联盟资产弹窗和确认层另在 1440×900、1920×1080 通过 Playwright，未出现横向溢出、console error 或 page error。最新证据见 `design-qa.md`。
- `/game/rewards` 奖励中心已按用户确认的视觉实现：只显示目标最小身份，不重复玩家在线状态、位置或个人资产；支持 Credits 与七种矿物组成固定礼包，通过游戏内邮件或直接到账批量发放，最多 50 名玩家。
- 奖励批次使用持久化父 Operation 和逐人子 Operation、精确确认文本、CSRF、幂等键、固定额度上限及不自动重试策略。2026-09-09 正式 Galaxy 玩家 #1 实测游戏内邮件投递成功并通过唯一 Mail ID/附件复核；直接到账实测 Credits 5001→5002；重复幂等键不重复执行，冲突请求返回 409，未登录/错误确认/重复目标均被拒绝。
- 奖励中心在 1440×900、1920×1080 完成同屏设计对照、核心交互和 Playwright 检查，无横向溢出、console error 或 page error；证据见 `design-qa.md`。
- 玩家和联盟 Inventory 已放入各自现有详情，不新增重复页面：0.10.0 除真实容量、槽位、数量、物品类型、名称和稀有度外，还读取静态图标路径；炮塔读取武器类型/分类、槽位类型、材质、平均/最高科技、DPS、伤害、射程、射速、精准度、槽位、尺寸、武器数量、武装/工业、同轴、追踪与连续光束。系统插件继续返回原版脚本与游戏实际 Seed；每次可从 11 个固定原版非剧情系统插件中选择 1 个、选择普通至传奇稀有度后发放。写入要求精确确认、CSRF、幂等键、持久 Operation、请求 Seed 关联与实际 Seed 的精确库存增量复核，不自动重试。
- 2026-09-09 正式 Galaxy 实测：玩家 #1 Inventory 从 2 个炮塔槽位读出，并成功加入普通能量增幅插件；联盟 #5 成功加入普通货舱扩展插件。两次 Operation 均为 `succeeded`、beforeCount 0→afterCount 1，同幂等键重放返回同一 Operation；非法剧情插件返回 400，未登录读取返回 401。初次实测暴露 Avorion 会将字符串 Seed 转换为内部数值 Seed，已改为同时关联请求 Seed并校验游戏实际 Seed，防止“已写入却误报失败”。
- 2026-09-10 现场升级并验证 0.10.0：玩家 #1 的两门真实炮塔分别识别为 `MiningLaser` 与 `ChainGun`，上述核心参数均通过 RCON、Agent 严格协议校验和 HTTP Inventory 合同返回；Avorion `title` userdata 已安全回退到物品 `name`，不会把内存地址显示到页面。1920×1080 与 1440×900 Playwright 均显示两门炮塔及材质/科技/DPS/射程，无横向溢出、console error、page error 或失败响应。
- 2026-09-10 已在同一玩家/联盟 Inventory 组件内加入共享物品目录：固定登记 19 种原版普通炮塔和 39 个原版系统插件，区分 11 个“已验证可发放”、33 个“仅目录”和 14 个“剧情禁止”。后端运行时发现 Avorion 客户端并建立只读图标白名单，本机索引 922 个通过扩展名、大小、重解析点和文件签名校验的图标；目录 58 个条目均成功显示真实静态图标。图标端点不接受任意磁盘路径或路径穿越。
- 联盟资产弹窗内的 Inventory 在 1440×1000、1920×1080 通过真实数据 Playwright 检查：容量、物品、白名单选择和二次确认可见，无横向溢出、console error 或 page error。

## 尚未完成

- Inventory 当前只对 11 个已验证系统插件开放发放；共享目录已覆盖 19 种普通炮塔和 39 个系统插件，但 `TurretTemplate`、`VanillaItem`、`UsableItem` 的完整定义目录尚未建立。炮塔更细的能耗/过热/效率/船员需求、19 种普通炮塔生成预览与发放仍未实现；具体炮塔三维缩略图也没有官方文件导出链路。边界见 `08_INVENTORY_ITEM_CATALOG_AND_ICONS.md`。
- 多玩家分页以及游戏内玩家直接调用管理命令的现场拒绝检查尚未完成。
- 游戏管理目前仅完成奖励中心；活动中心、星区建设和活动记录尚未开发。舰船管理正式 UI/完整后端尚未开发；踢出、封禁、私信、联盟修改等写操作尚未授权或实现。
- Steam Query 27020 虽已由游戏监听并连接 Steam，但标准 A2S 查询仍超时；控制页在线人数已在该协议不可用时使用 OrionAdminBridge 的真实数据回退，并明确保留 Steam Query 降级状态。
- 星区读取与单个空闲星区手动卸载、内存告警策略持久化、备份恢复和精确 PID 强制终止已接入并实测。自动批量卸载、临界自动重启和完整安全更新仍保持关闭。
- 公网多用户/正式权限系统、HTTPS、多节点部署、稳定服务安装和完整发布流程仍需后续规划/验收。
- 不保证现有全部按钮已完成；逐项审查，未接入清楚显示状态。
- 部分现有页面/历史截图仍有旧“系统不会植入管理 MOD”文案，应在下一次相关功能修改时按最新专用 MOD 授权核对调整；本次迁移不修改业务源码或重做 UI。

## 最近一次用户问题与准确证据

用户没有公网 IP，尝试启动另一份默认 `avorion_galaxy`，日志显示 Steam 初始化失败、随后 bind 10048 端口冲突，启动退出。
旧机的隔离 `MachineAcceptance` 当时实际正常运行，MOD 已加载；应由 Steam 启动游戏客户端，在多人游戏中加入测试服，不应再开一份默认服务端。
本机 127.0.0.1 连接不要求公网 IP；Steam 认证联网是另外的条件。客户端连通性仍未实测，不能因 RCON 成功就说游戏一定能加入。
旧机实测游戏端口 27100，但查询端口并非自动跟随：日志记录 Query 27003、Steam Query 27020、Steam 27021、RCON 27115。新电脑必须检查实际端口，不能直接猜 game+3，更不能据此直接改代码中的未验证端口选项。

## 下一步建议（在用户恢复开发后执行）

1. 开发 19 种普通炮塔的“生成预览 → 二次确认 → 单件发放”；先在隔离 Galaxy 验证材质、科技、稀有度、Seed、DPS 预算、库存增量和重启保存。
2. 开发舰船详情、货舱和船员补充；先验证真实离线舰船、归属和容量边界。
3. 开发海盗事件与星区建设模板前，先完成安全生成、清理和重启恢复的隔离服验证。
4. 继续定位 Avorion 标准 A2S 不响应的原因，但不得用假 Query 数据替代已明确显示的降级状态。

当前本机管理后台与正式本机 Avorion 服务已运行；文档不固化 PID，恢复操作时必须重新读取进程、端口和运行档案。
